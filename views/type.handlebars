<section id="type_tasks" class="container">
    <h2>Все типы заданий</h2>
    <div class="d-flex justify-content-end">
        <button class="btn btn-success btn-md bt" data-toggle="modal" data-target="#myModalAdd">Добавить тип задания</button>
    </div>
        <table class="table table-striped" id="mytable">
            <thead>
                <tr>
                <th>Тема </th>
                <th>Тип задания</th>
                <th style='text-align:right'>Действие</th>
                </tr>
            </thead>
            <tbody>
                {{#each types}}
                <tr>
                <td>{{ theme.name }}</td>
                <td>{{ name }}</td>
                <td align='right'>
                    <a href="javascript:void(0);" class="btn btn-sm btn-info edit" data-id="{{ id }}" data-themeId="{{ themeId }}" data-name="{{ name }}">Edit</a>
                    <a href="javascript:void(0);" class="btn btn-sm btn-danger delete" data-id="{{ id }}">Delete</a>
                </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
        
     </div>

         <!-- Modal Add Type-->
      <form action="/admin/type/add" method="post">
        <div class="modal fade" id="myModalAdd" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
         <div class="modal-dialog" role="document">
           <div class="modal-content">
             <div class="modal-header">
               <h5 class="modal-title" id="exampleModalLabel">Добавить новый тип задания</h5>
               <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                 <span aria-hidden="true">&times;</span>
               </button>
             </div>
             <div class="modal-body">
               <div class="form-group">
                 <span>Тема:</span>
                   <select name="themeId" class="form-control">
                      {{#each themes}}
                         <option value="{{id}}">{{name}}</option>
                      {{/each}}
                   </select>
               </div>
 
               <div class="form-group">
                   <input type="text" name="name" class="form-control" placeholder="Тип задания" required>
               </div>
             </div>
             <div class="modal-footer">
               <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
               <button type="submit" class="btn btn-primary">Save</button>
             </div>
           </div>
         </div>
        </div>
     </form>

     <!-- Modal Delete Product-->
      <form id="add-row-form" action="/admin/type/delete" method="post">
         <div class="modal fade" id="DeleteModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
            <div class="modal-dialog">
               <div class="modal-content">
                   <div class="modal-header">
                                        <h5 class="modal-title" id="myModalLabel">Удаление типа</h5>
                      <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                   </div>
                   <div class="modal-body">
                                                 <strong>Вы действительно хотите удалить данный тип задач?</strong>
                   </div>
                   <div class="modal-footer">
                                            <input type="hidden" name="id" class="form-control id" required>
                        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                        <button type="submit" class="btn btn-success">Delete</button>
                   </div>
                    </div>
            </div>
         </div>
     </form>

     <!-- Modal Update Product-->
   <form action="/admin/type/update" method="post">
       <div class="modal fade" id="EditModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
          <div class="modal-content">
            <div class="modal-header">
              <h5 class="modal-title" id="exampleModalLabel">Edit Product</h5>
              <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
              </button>
            </div>
             <div class="modal-body">
               <div class="form-group">
                   <select name="themeId" class="form-control">
                      {{#each themes}}
                         <option value="{{id}}">{{name}}</option>
                      {{/each}}
                   </select>
               </div>
               <div class="form-group">
                   <input type="text" name="name" class="form-control name" placeholder="Тип задания" required>
               </div>
             </div>
            <div class="modal-footer">
              <input type="hidden" name="id" class="id">
              <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
              <button type="submit" class="btn btn-primary">Update</button>
            </div>
          </div>
        </div>
       </div>
  </form>

<script>
    $(document).ready(function(){
            //showing data to edit modal
          $('#mytable').on('click','.edit',function(){
        var themeId = $(this).data('themeId');
        var name = $(this).data('name');
        var id = $(this).data('id');
        $('#EditModal').modal('show');
        $('.name').val(name);
        $('.themeId').val(themeId);
        $('.id').val(id);
      });
            //showing delete record modal
            $('#mytable').on('click','.delete',function(){
        var id = $(this).data('id');
        $('#DeleteModal').modal('show');
        $('.id').val(id);
      });
 
    });
</script>

</section>  